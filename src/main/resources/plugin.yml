name: GitCraft
version: '${version}'
main: com.gitcraft.GitCraft
api-version: '1.21'
author: marcus
description: Git-like version control for Minecraft builds.
depend: [WorldEdit]
commands:
  gitcraft:
    description: GitCraft root command.
    usage: /gitcraft <select [name]|pos1|pos2|clear|commit <msg>>
    permission: gitcraft.use
permissions:
  gitcraft.use:
    description: Use GitCraft commands.
    default: true
